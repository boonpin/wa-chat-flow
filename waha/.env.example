# Secrets for the WAHA gateway. Generate each with: openssl rand -hex 32

# Protects the REST API. Must match WAHA_API_KEY in the application's .env.
WAHA_API_KEY=

# Web dashboard login (browser basic auth). Unrelated to the API key above.
WAHA_DASHBOARD_USERNAME=admin
WAHA_DASHBOARD_PASSWORD=
